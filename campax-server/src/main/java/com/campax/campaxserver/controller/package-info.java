/**
 * REST controllers. Thin by design: request/response mapping and HTTP
 * semantics (status codes, Location headers, pagination params) only -
 * all business logic lives in {@link com.campax.campaxserver.service}.
 */
package com.campax.campaxserver.controller;
