/**
 * Business logic and transaction boundaries. This is where mapper output
 * gets assembled with repository lookups (resolving ids to entities) and
 * where {@link com.campax.campaxserver.customfield.CustomFieldJsonService}
 * conversions happen for staff-related resources - concerns the mapper
 * layer deliberately stays out of.
 */
package com.campax.campaxserver.service;
