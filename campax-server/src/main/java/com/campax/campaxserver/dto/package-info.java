/**
 * Data transfer objects exposed at the API boundary, grouped by
 * aggregate (student, guardian, school, role, permission, staff).
 * Implemented as Java records: immutable, and cheap to construct in
 * mappers and tests.
 */
package com.campax.campaxserver.dto;
